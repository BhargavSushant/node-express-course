const printStakeHolders = (names) => {
  console.log(
    `Project ${names.project} is being managed by ${names.manager} & being made by ${names.programmer}`
  );
};

module.exports = printStakeHolders;
